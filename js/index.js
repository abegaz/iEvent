$(document).ready(function () {

  $('#calendar').fullCalendar({

  });
  $('#calendar').fullCalendar({
    themeSystem: 'bootstrap4'
  });
});

// Parse 
function jsonToEvents(json){

}

function addEvent(name, startDate, endDate, href){

}